contract('GlobalTradeSystem', function(accounts) {
  it("should assert true", function(done) {
    var global_trade_system = GlobalTradeSystem.deployed();
    assert.isTrue(true);
    done();
  });
});
